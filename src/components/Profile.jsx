import { Navigate } from "react-router-dom"

const Profile = ({children}) => {

    return (
        <>
            You are authenticated and can access your profile
        </>
    )
}

export default Profile